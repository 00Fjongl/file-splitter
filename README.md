# file-splitter
An extremely barebones file splitter. Files split with this have their bits reorganized so that file headers don't get mistakenly recognized.  
This can be used to stay under file upload size limits.
